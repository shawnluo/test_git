/*  meta

In an int array, if there are three elements a, b, c that satisfies a * a + b * b = c * c, we will return true.
For example, [0, 1, -2, 3, 4, 5],  3, 4, 5 is the satisfying element.
So we say array [0, 1, -2, 3, 4, 5] will return true
Also [0, 0, 0], [0, 1, -1] will return true
[0, 1, 0] will be false.
Write a solution for this algorithm
注意这里的array可以重复 而且是有正负号的

*/